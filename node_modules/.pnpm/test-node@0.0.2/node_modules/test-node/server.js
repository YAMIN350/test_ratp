var user = require('./user');
var log = require('./logger')(module);
var db = require('db');

function run() {
  var vasja = new user.User("Вася");
  var petja = new user.User("Петя");

  vasja.hello(petja);

  log(db.getPhrases('Hello'));
}

if (module.parent) {
  exports.run = run;
} else {
  run();
}
