// Export
var db = require('db');
db.connect();

var log = require('../logger')(module);

function User(name) {
  this.name = name;
}

User.prototype.hello = function(who) {
  log(db.getPhrases('Hello') + who.name);
};

console.log('Файл подключен Ёпта!');

exports.User = User;
